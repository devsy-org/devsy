version: '3'

services:
  app:
    image: ghcr.io/devsy-org/test-images/go:1
    command: sleep infinity
    volumes:
      - .:/workspaces:cached
  nginx:
    image: ghcr.io/devsy-org/test-images/nginx-unprivileged:latest
