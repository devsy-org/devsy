// Package installscript tests the published Devsy CLI install script
// (sites/docs-devsy-sh/public/install.sh) end to end against a fake
// release server, so the file served at https://devsy.sh/install.sh
// keeps working as releases and platforms evolve.
package installscript
