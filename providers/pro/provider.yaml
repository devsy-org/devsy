name: devsy-pro
version: v0.0.1
icon: https://devsy.sh/assets/devsy.svg
home: https://github.com/devsy-org/devsy
description: Devsy Pro
options:
  DEVSY_CONFIG:
    global: true
    hidden: true
    required: true
    default: "${PROVIDER_FOLDER}/devsy-config.json"
exec:
  daemon:
    start: ${DEVSY} pro daemon start
    status: ${DEVSY} pro daemon status
