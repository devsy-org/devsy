---
kind: task
id: ci-optimizer
agent: ci-optimizer
---

Run the `ci-optimizer` agent against `.github/workflows` and recent Action logs.
Produces one small CI improvement per run and persists findings to the running automation via the automation service.
