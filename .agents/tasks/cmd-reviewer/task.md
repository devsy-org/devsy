---
kind: task
id: cmd-reviewer
agent: cmd-reviewer
---

Run the `cmd-reviewer` agent against one `cmd/` subdirectory. Produces one small CLI
improvement per run and persists findings to the running automation via the automation service.
