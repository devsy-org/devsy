---
kind: task
id: pkg-ssh-git
agent: pkg-ssh-git
---

Run the `pkg-ssh-git` agent against one package in its scope. Produces one small focused
improvement per run and persists findings to the running automation via the automation service.
