---
kind: task
id: ui-polish
agent: ui-polish
---

Run the `ui-polish` agent against the devsy desktop UI (`desktop/src/renderer/src/`).
Produces one small, self-contained UI improvement per run and persists findings to the
running automation via the automation service.
